import { FormatImages } from './shared';

type PageCharacteristicType = {
  title: string;
  desc: string;
  order: number;
  link: string;
  coverImage: FormatImages;
};

export default PageCharacteristicType;
